# ============================================================
# Content Agent — 项目直接依赖
# 间接依赖由各包自行管理，不在此列出
# ============================================================

# Agent 框架
crewai>=1.14.0
crewai-tools>=1.14.0

# Web 框架
fastapi>=0.115.0
uvicorn>=0.34.0
python-multipart>=0.0.18      # FastAPI 文件上传
sse-starlette>=3.0.0          # Server-Sent Events（WebSocket 降级方案）

# 数据模型 & 配置
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0          # .env 文件加载

# 数据库
psycopg2-binary>=2.9.0        # PostgreSQL 连接（同步，Agent/工具用）
asyncpg>=0.30.0               # PostgreSQL 异步连接（FastAPI 用）
redis>=7.0.0                  # Redis 缓存
bcrypt>=4.0.0                 # 密码哈希验证

# LLM API 调用
openai>=1.60.0                # OpenAI 兼容接口（MiniMax/Mimo/DeepSeek/Qwen）

# 本地 Embedding（CPU）
torch>=2.5.0 --index-url https://download.pytorch.org/whl/cpu
sentence-transformers>=3.0.0  # bge-large-zh-v1.5 编码
numpy>=1.26.0                 # 向量计算

# 云服务
cos-python-sdk-v5>=1.9.0     # 腾讯云 COS 对象存储

# 认证
PyJWT>=2.10.0                 # JWT token 生成/解析

# 文件解析
python-frontmatter>=1.0.0     # Obsidian Markdown frontmatter 解析

# HTTP 客户端
httpx>=0.28.0                 # 异步 HTTP 请求

# 工具库
PyYAML>=6.0                   # YAML 配置解析
regex>=2024.0.0               # 正则表达式（合规校验）
tenacity>=9.0.0               # 重试逻辑
watchdog>=5.0.0               # 文件系统监控（Obsidian 同步）

# 监控
prometheus-client>=0.21.0     # Prometheus 指标采集

# 测试（开发依赖）
pytest>=8.0.0
